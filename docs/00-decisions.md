# 决策记录

> 记录"为什么这么定"，不是"做了什么"。过两周忘了当初的理由时看这里。
> 每条决策都写清楚**代价**和**什么情况下要推翻它**。
>
> 起始：2026-08-05

---

## 背景：两类用户

整个项目的所有决策都绕着这个分类转，先明确：

| | A 类 | B 类 |
|---|------|------|
| 谁 | 作者本人这样的人 | 朋友、读者 |
| 特征 | 有 Obsidian、有自己的 AI Agent、愿意折腾 | 只想"对着微信说话，日记自动出现" |
| 要什么 | 数据契约稳定、可 hack | 装得上、用得起来 |
| 卡在哪 | 不卡 | `pip install` 报错就流失 |

**019 (Python 版) 是给 A 类的**，README 甚至是写给 A 类用户的 Agent 看的。
**020 (插件版) 是为了拿下 B 类。**

⚠️ 最大的未验证假设：**做出来到底有没有人连续用 30 天。**
作者自己都用不起来（见 D3）。所有工程投入的优先级都应该服从于"尽快验证留存"。

---

## D1 · 产品形态选 Obsidian 插件

**决策**：第一阶段做 Obsidian 插件，不做独立 exe/app。

**为什么不是别的形态**（这一步是物理约束，没得选）：

要做到「微信输入 → 用户自己电脑的本地 markdown」，链条上必须有个东西在**用户电脑上运行**。所以：

- ❌ **网页**：浏览器写不了任意本地文件（File System Access API 要每次授权、Safari 不支持、关页面就停）
- ❌ **小程序**：微信沙盒里，碰不到电脑
- ❌ **手机 App**：写不到电脑硬盘
- ❌ **纯云端**：违背"数据不上服务器"的底线

所以本地常驻程序是**必需的**，真正的问题只是：**它寄生在什么宿主里。**

**为什么是 Obsidian 而不是自己打包 exe**：

| | 自己打包 exe/app | Obsidian 插件 |
|---|---|---|
| 安装 | 下载→双击→过安全提示→配自启 | 市场里搜索→点安装 |
| 开机自启 | 要自己做 | Obsidian 开着就在跑 |
| 更新 | 要自己做 | 市场自动更新 |
| 签名 | mac 需 Apple 开发者账号 $99/年，Win 会报毒 | 无 |
| 跨平台 | Win/mac 各打一份 | Obsidian 已经解决 |
| vault 路径 | 要问用户 | 天生就在 vault 里 |

**插件形态把安装、自启、更新、签名、跨平台这五个最烧人的工程问题一次性外包给了 Obsidian。**

而且作者已经上架过 Obsidian 插件（`memo-lite`），分发流程趟过一遍。

**代价（必须认）**：
- 第一批用户**必须是 Obsidian 用户**。不用 Obsidian 的人，要先装一个 100MB 的软件才能用日记工具——对他们来说插件形态**反而更重**。
- Obsidian 不开着就收不到消息（但 cursor 机制应该能补拉，**待实测**）。

### 📌 2026-08-05 调研补充 —— 三条必须记进代价栏的确认结论

**① 技术上没有阻断性问题，审核大概率能过。【已确认】**
- 提交流程 2026-05 已改版：不再向 `obsidian-releases` 提 PR，改为在 `community.obsidian.md`
  开发者面板提交，**几分钟出结果**，通过后 24 小时内可搜到
- 政策对"连第三方服务器 + 传输用户内容"是 **allow-with-disclosure**：
  原文 "The following are only allowed if clearly indicated in your README"，
  含 "Network use. Clearly explain which remote services are used and why they're needed"
- Not allowed 黑名单（74 行）**无任何一条**针对第三方服务 / IM 集成 / LLM 调用
- 在架先例：`wechat-inbox-sync`（域名含 `channels.weixin.qq.com`）Review 为 Satisfactory；
  `telegram-sync`（270 issues + eval + 8 个外部域名）Review 为 Caution，**经人工审核通过**，
  48k 下载；**你自己的 `memo-lite` 440 issues 也照样在架**
- → **扫描结果是评分卡，不是准入闸门**

**② 🔴 Obsidian 关掉 = 插件完全停止，收不到任何消息。【已确认】**
源码级确认：`app.on("window-all-closed", () => { isMac || app.quit() })`。
Windows/Linux 关最后一个窗口即退出；macOS 不退但所有 BrowserWindow 已销毁 →
渲染进程没了 → 插件 JS 停止。

**这和当前 Python 版可独立常驻是本质差别，是我当初推荐插件形态时低估的一条。**
社区的 tray 类插件只是让窗口保持存在（从而落回后台节流），不是让插件脱离 Obsidian 跑；
且 `obsidian-tray` 依赖 Electron **已弃用**的 `remote` 模块，最后实质更新 2025-03。

**"开着"到底指什么 —— 精确定义**：

| 场景 | Python 版 (019) | 插件版 (020) |
|------|:---:|:---:|
| 电脑关机 | ❌ 停 | ❌ 停 |
| **电脑睡眠** | ❌ 停 | ❌ 停 |
| 电脑醒着，Obsidian 没开 | ✅ 跑 | ❌ 停 ← **唯一的差别** |
| 电脑醒着，Obsidian 最小化 | ✅ 跑 | ✅ 跑（有节流，但 HTTP I/O 在主进程，不受影响） |
| 电脑醒着，Obsidian 开着 | ✅ 跑 | ✅ 跑 |

- **睡眠不算开着**：整个系统冻结，所有进程停，网络连接断。
  这一点两个版本完全一样——019 的 README 自己也承认"电脑睡眠时提醒也发不出去"。
- **所以两种形态的真实差距只有一行**：电脑醒着但没开 Obsidian。
  对常年开着 Obsidian 的用户，这一行的杀伤力有限。
- ⚠️ **macOS 特有待测**：关窗口（Cmd+W）后 app 仍在 Dock，但源码显示
  BrowserWindow 已销毁 → 渲染进程没了 → **插件可能已经停了**。需要实测确认。

**🔴 由此推出一个更要紧的结论**：既然睡眠 = 停，那用户**每晚睡觉时电脑也睡觉，
每天都有 8+ 小时离线**。早上在床上用微信说的话，电脑还睡着，当场收不到。

**这不是插件版特有的问题，Python 版一模一样。**
→ **P0 实测①（cursor 能补拉多久）比原先以为的更关键**：
它不只关系到"要不要开 Obsidian"，而是关系到**每一天的常规使用**。
如果 cursor 只能补几分钟，那这个产品对"早上想记两句"的场景根本不成立。

**③ 移动端出局，`isDesktopOnly: true`。【已确认】**
移动端完全没有 Node.js / Electron API，`requestUrl` 走 Capacitor 原生实现，
桌面端结论不能外推；iOS/Android 会挂起后台应用（OS 级约束，Obsidian Sync 的
移动端后台同步 feature request 从 2021-10 开到现在仍未实现）。
同构先例 `obsidian-telegram-sync` 的 README 原文：
"would never be available on mobile platforms"。

**④ 用 Node `https` 而不是 `requestUrl`。【已确认】**
`requestUrl` 底层 Electron `net.request` **强制跟随系统代理，无法逐请求 opt-out**
（唯一后门 `session.defaultSession.setProxy()` 会影响整个 Obsidian）。
国内用户开着 Clash 时 iLink 请求会被强制走代理——而 019 花大力气做的正是反向的事。
合规没问题：用 Node API 只要求 `isDesktopOnly: true`，反正②③已经决定了这一点。

**为什么接受这个代价**：
在留存假设没被验证之前，花几周解决 mac 签名和开机自启，是在给一个还没证明有人要的东西铺路。而且 **Obsidian 用户是最可能持续记日记的人群**（本来就有记录习惯），从最容易转化的人群开始验证，成本最低、信号最准。

**什么时候推翻**：
插件版验证出留存后，再做独立 exe/app 覆盖非 Obsidian 人群。两种形态产出**完全一致的 markdown**，用户随时可以在两边迁移。

---

## D2 · 内建回顾能力（改变项目定位）

**决策**：020 内建"回顾/检索"，不再沿用 019 的"只做管道"定位。

**为什么**：
019 的 README 写着 *"回顾/检索/周报交给你自己的 Agent 在 vault 上做，本项目不做也不干涉"*。这句话是**写给 A 类用户的**——但：

> **B 类用户没有自己的 Agent。**

对他们来说这些功能等于不存在，产品就只剩一个录音笔：说话、存文件、没了。没有任何东西让他们明天再来。

**一石三鸟**：
1. 让它真的像个 Agent，而不是管道
2. 给 B 类用户明天再来的理由
3. **用户主动来问，就刷新了 context_token 的时间窗口**（见 `protocol-notes.md` 第 4 节）——回顾功能本身就是提醒问题的解药

**技术上很便宜**：日记就是本地 md，一个月也就几万字，直接读出来塞进 LLM 上下文。
**不需要向量库、不需要 RAG、不需要服务器。** 插件形态下读 vault 文件是原生能力。

**"Agent 感"具体是什么**（按价值排序）：
- 主动性：「昨天你说的那个面试，后来怎么样了？」← **这一句胜过前面所有功能**
- 「上周我都干了啥」→ 读本地 md → LLM 给一段回顾
- 「我最近是不是老在抱怨同一件事」→ 它真的看得出来

**代价**：需要 AI key 才能用（零 key 模式下回顾能力不可用，只能降级成简单的按日期列出）。
019 的"零 key 也完整可用"承诺，在 020 里要重新表述。

---

## D3 · 提醒做多路冗余，不指望微信推送

**决策**：不把「微信定时推送」当作留存的主力机制。

**为什么**：

**① 真正的死结（2026-08-05 调研确认，比原先以为的更糟）**：

```
用户每天写  → token 天然高频刷新 → 提醒发得出去（但他本来就不需要提醒）
用户连续没写 → token 陈旧        → 提醒发不出去（而这正是最需要提醒的时刻）
```

**提醒在最需要它的时刻必然失效。** 叠加"每天固定同一时钟、间隔接近 24 小时"，
越规律越发不出去。这就是"装了但用不起来"的技术根因。

**② 🔴 原来写的"20 小时限制"是编的。【已确认】**
腾讯官方源码、README、CHANGELOG、全部社区文档**没有一处提到 20 小时**，
"平台反骚扰规则"这个定性同样零出处。代码层面可确证它是本地拍脑袋的启发式：
`_is_token_fresh` 只做本地时间戳相减，**不发任何网络请求**，
而判定不 fresh 时**直接跳过、根本不调 sendmessage**——
这条链路从未、也不可能从服务端观测过真实窗口。
（社区流传的"24 小时"同样是假的，详见 `protocol-notes.md` 3.2。）

**真实窗口没有任何人知道。** 所以第一件事是**把预判删掉，改成"到点就发、按返回码记录"**——
现在是"我猜发不出去所以不发"，连试都不试，白白放弃了可能能发出去的时段。

**③ iLink 通道内确实没有换取推送额度的机制。【较可信】**
已知 9 个端点里没有模板消息、没有订阅授权、没有一次性推送凭证。
`sendmessage` 是唯一出口，而它依赖只能由用户入站产生的 `context_token`。
——但注意：**理由是"没开放这类接口"，不是"腾讯反对主动推送"**。
企业微信自建应用的推送侧就没有任何时间窗口（见下）。

**方案：三路冗余**

| 优先级 | 通道 | 可靠性 | 代价 |
|---|---|---|---|
| 主力 | **系统日历/提醒事项** —— 安装引导时让用户在手机上加一条每天 22:00 的重复提醒 | 100%，永不失效 | 土，要用户手动加一次 |
| 增强 | **iLink 推送** —— 删掉时间预判，到点就发、按返回码分流记录 | 尽力而为 | 真实成功率未知 |
| 辅助 | ~~Obsidian 本地通知~~ | ❌ **已否决** | 见下 |

⚠️ **"用 Obsidian 本地通知替代微信推送"这条被调研否决了**：Obsidian 的系统通知
只在 Obsidian 运行时触发，**且移动端根本不提供系统通知**（Reminder 类插件的长期已知限制，
社区解法是转投 ntfy 等外部服务）。对"随时随地用手机记日记"的用户，
这是把"窗口可能失效"换成了"手机上必然收不到"——**更差的一侧**。

**核心心智**：别指望单一通道解决留存。

**另一半答案在 D2**：回顾能力带来的"主动想来"，比任何推送都可靠。

### 🔑 2026-08-05 追加核实：提醒可能没那么难修

直接读腾讯官方源码后确认（详见 `protocol-notes.md` 3.4）：

- 官方的 contextToken store 是个**纯 `Map<string,string>`**——
  **没有时间戳、没有 TTL、没有过期逻辑**（grep `ttl|expire|stale|Date.now()` 命中数为 0）
- 它被**特意落盘**，注释写明是为了 "survive gateway restarts"——官方不但不丢旧 token，
  还专门保证跨重启存活
- 没有 token 时官方**只 warn 然后照发**，不拦截
- 官方 prompt 里明确指导怎么给微信用户**建 cron 定时任务**——
  定时主动推送是官方设计里的正常用例

**→ 019 的 `_is_token_fresh` 是官方实现里根本不存在的发明，
而它正是"连试都不试就跳过提醒"的那个开关。**

**第一优先级的修复因此变成：把这个预判整个删掉。** 有可能这一条就把提醒修好了。
在做任何多路冗余之前，先做这个。

### ❌ 企业微信旁路 —— 已放弃（2026-08-05）

调研发现**企业微信自建应用的推送侧没有任何时间窗口**【已确认，逐字核对
`developer.work.weixin.qq.com/document/path/90236`】：限制纯粹是流量阈值型
（每应用「账号上限数×200 人次/天」，对同一成员「30 次/分钟、1000 次/小时」），
**通篇无"用户须先互动"之类的条款**。这证伪了"微信一概反对主动推送"的直觉。

**但这条路已放弃，不做实测。** 决定于 2026-08-05，理由按强度排序：

1. 🔴 **即使技术上通，接入流程对 B 类用户完全不可能**：注册企业微信 →
   创建自建应用 → 配置企业可信 IP → 拿 corpid/secret → 成员关注微信插件。
   **这和 020 的存在理由（让不懂技术的普通人能用）直接矛盾。**
   而且"可信 IP"很可能要求固定 IP，家用动态 IP 直接出局——除非架中转服务器，
   那又违背"数据只在本机"。
2. **它只能解决"出站提醒"，收不了用户输入**（需要公网 HTTPS 回调）。
   最多是 iLink 的补充，不是替代，收益本来就有限。
3. 作者判断：企业微信与普通微信之间的接口只有"企业成员加微信用户为外部联系人聊天"，
   群机器人的消息不会落到普通微信里。
   （补充：企业微信历史上确有"微信插件"机制，Server酱这类推送服务用过它——
   但 2026 年是否还通没有核实，**且不影响上面第 1 条结论**。）
4. 这个调研方向的**核查 agent 本来就失败了**，全部内容标的是【未核查】，
   本来就不该当结论用。

**→ 提醒问题回到 iLink 通道内解决，见上面「追加核实」一节。**

---

## D4 · 日记只存一份，在用户指定的 vault 目录

**决策**：日记内容保存在用户在插件设置里指定的 vault 内目录，**只此一份**。
不做副本、不做云备份、不在插件目录里另存一份。

**为什么**：备份交给用户自己的同步盘（iCloud / OneDrive / 坚果云 / Obsidian Sync）。
多处保存 = 多处不一致 = 用户不知道哪份是真的。

**具体布局**：

```
用户的 vault/
├── .obsidian/plugins/wechat-diary/
│   ├── main.js / manifest.json / styles.css
│   └── data.json          ← 配置 + iLink token（见 D5 ⚠️）
└── 日记/                   ← 用户在设置里选，插件给个默认值
    └── 2026/
        └── 2026-08-05.md   ← 格式和 Python 版一字不差（见 D6）
```

**和 019 的差异**：019 用 `.env` 里的绝对路径 `DIARY_DIR`；020 用 **vault 内相对路径**。
因为插件天生活在 vault 里，而且只有走相对路径才能用 `app.vault` API
（能被 Obsidian 正确索引、触发链接更新、参与同步）。

数据契约里 `YYYY/YYYY-MM-DD.md` 的结构**一个字不改**，只是根从绝对路径变成 vault 相对路径。

---

## D5 · 运行时状态不落在 vault 可见区域

**决策**：iLink token、session 状态、用户 profile 一律走 Obsidian 官方的
`this.saveData()` / `loadData()`，写到 `.obsidian/plugins/wechat-diary/data.json`。

**为什么**：019 的 `data/*.json` 如果照搬到 vault 的普通文件夹，会被 Obsidian 索引、
出现在搜索结果里、被同步盘同步、甚至在图谱视图里冒出来。

**✅ 2026-08-05 调研给了官方答案：用 `SecretStorage`，不要用 `saveData` 存 token。**

- `saveData()` 写的是 `<configDir>/plugins/<id>/data.json`，**明文 JSON、在 vault 目录树内**。
  任何文件夹级同步（iCloud / Dropbox / Syncthing / Git）都原样带走，Git 场景极易误提交。
- **正确做法**：Obsidian 1.11.4+ 的官方 `SecretStorage` API
  （`app.secretStorage.getSecret / setSecret`），走 Electron `safeStorage`
  （macOS Keychain / Windows DPAPI / Linux libsecret）加密后存进 Electron localStorage，
  **不在 vault 里**，按 vault 隔离。
- 代价：需要 `minAppVersion ≥ 1.11.4`，老版本要有 data.json 降级路径。
  ⚠️ 一个坑：`isEncryptionAvailable()` 为 false 时 Obsidian **会明文落盘**，要处理这个分支。

**→ 决策：token 走 SecretStorage，其余非敏感配置（日记目录、提醒时间）留在 data.json。**

### 🔴 补记 2026-08-14 —— 这条决策制造了一个线上故障，v0.2.1 修

上面这个决策本身是对的，但它带来一个当时没想到的后果：**一份"绑定"被劈成了两半，
存在两个生命周期完全不同的地方**，而代码从头到尾假设两半同生共死。

| | 卸载插件 | 随 vault 同步 | 换设备/换路径 |
|---|---|---|---|
| `bot_token`（SecretStorage） | **不删** | **不走** | **丢** |
| `userId`/`botId`/`baseUrl`（data.json） | **删** | **走** | 跟着 vault |

任一半单独消失 → "半绑定"，而 v0.1.3 里没有任何一条路能走出来：
`bound = token && userId` 二值判定把它错判成"未绑定" → 解除绑定按钮被禁用 →
残留 token 又被塞进 `local_token_list` → 重新扫码必被服务端顶回 `binded_redirect` →
而那个分支的文案还是错的（见 `protocol-notes.md` 勘误）。**用户被锁死。**

**SecretStorage 的存储位置与隔离粒度（逆向 Obsidian asar 实测，1.13.x）**：
`safeStorage.encryptString()` → `app.saveLocalStorage("secrets-encrypted")` →
`localStorage["<appId>-secrets-encrypted"]`，落在 `%APPDATA%\obsidian\Local Storage\leveldb`。
**`appId` 是按 vault 的绝对路径登记的**（`obsidian.json` 的 `vaults` 映射），
且 `.obsidian/` 里没有任何携带 vault id 的文件。推论：

- 库路径变了（OneDrive 已知文件夹移动、换盘、改用户名、打开的是同步冲突副本）
  → 新 appId → **旧 secret 变孤儿**，而 data.json 跟着 vault 好端端地来了 = 半绑定。
- 换机器 → secret 为零，data.json 却随同步盘到了 = 半绑定。
- 卸载插件 → data.json 没了，secret 还在 = 半绑定（反过来的那一半）。

**修法（v0.2.1）**：
1. 绑定身份（userId/botId/baseUrl）**也存一份进 SecretStorage**（`wechat-diary-bind-identity`），
   与 token 同生命周期。data.json 丢了在 `onload` 里无感恢复。
2. 状态改三态 `bound / half / none`，半绑定不再伪装成未绑定，清理入口永远可用。
3. `binded_redirect` 按官方语义当成功；有 token 就启动管道，userId 为空时进
   "待认领"——**弹窗让人确认**才认主人（协议层没有 allowlist，不弹窗就是谁先说话谁当主人）。
4. 解除绑定拆成「只清主人身份（保留凭据）」和「彻底解除」。后者可能不可逆：
   服务端不补发凭据，清掉本机 token 后同一微信号重扫会被判 `binded_redirect`。
5. 从 secret 恢复身份时，`buf` 和 `recentSeqs` 必然是空的 → 服务端可能回吐积压，
   而写入用的是 `todayStr()`（报文里没有原始时间）→ 会把历史**按今天的日期重演一遍**，
   连「撤回」「结束」都会被重放。所以恢复后的第一波只推游标不落笔，等长轮询空一次再开始记。

**另外三条写文件的官方结论【已确认】**：
- API 顺位：`Editor` > `Vault.process` > `Vault.modify`，Adapter API 是最后手段
- ⚠️ **Obsidian 的"原子性"只是 in-process 串行队列**，底层 `fs.writeFile` 直接截断写，
  **没有 temp+rename** —— 崩溃/断电仍可能留半截文件。
  019 的 `写 .tmp + os.replace` 在 Obsidian API 里**没有对应物**，
  数据契约规则 2 要重新想怎么满足
- `Vault.create` 的 exists 检查和 write 不在同一事务内，有 TOCTOU 窗口
  → **按天建文件时应先 `getFileByPath`，拿到 TFile 就 append/process，拿不到才 create**
- 不要硬编码 `.obsidian`（用 `Vault.configDir`）；不要用 `vault.delete`（用 `trashFile`）

---

## D6 · 数据契约两边严格一致

**决策**：`019Dairy/docs/data-contract.md` 是**权威版本**，020 保存一份副本。
任何修改必须两边同时改。

**为什么**：两个实现产出的日记必须完全一致，否则用户从 Python 版切到插件版
（或反过来），日记格式就断层了。

**019 / 020 的关系是双向的**：
- 020 从 019 抄（文案、意图规则、写入逻辑、协议知识）
- 019 以后优化了，020 要跟进
- 020 优化了，也要反哺回 019

### ⚠️ 待办：契约 v1.1（图片）需要回灌 019 — 2026-08-12

020 v0.2.0 加了图片接收，契约相应加了**规则 7（图片块）**和 `attachments/` 目录，
已写进 020 的副本，**019 的权威版还没同步**。

这是本项目第一次出现 **020 先行、019 跟进** 的方向，所以特别记一笔：

- 变更是**向后兼容的增量** —— 019 不产出图片块，只要求它「读到 `![[...]]` 块时
  当普通消息，别当坏数据修」，而它的 `count_messages` 天然满足。
  所以两边产出的库仍可互相追加，**没有断层风险**，不是紧急项。
- 但契约文本必须回灌，否则「权威版」名不副实 —— 下次有人只读 019 的契约，
  会以为 `attachments/` 是野生目录。

---

## D7 · 命名（公开且几乎改不了，一次定死）

| 位置 | 值 | 理由 |
|------|-----|------|
| 本地文件夹 | `020obs-wechatdairy` | 保持作者 Windows 工作区的编号习惯，本地名随意 |
| GitHub 仓库 | `obsidian-wechat-diary` | 同账号已有 `wechat-diary`，**不能重名**；`obsidian-` 前缀是社区惯例 |
| `manifest.json` 的 `id` | `wechat-diary` | 官方指南要求插件 id **不含** "obsidian" 字样 |
| 显示名 `name` | `WeChat Diary` | 市场里用户看到的 |

⚠️ 公开的三个必须拼 **diary**（日记）不是 dairy（乳制品）。
插件 id 会变成用户硬盘上的真实路径 `.obsidian/plugins/wechat-diary/`，
装到几百人机器上之后再改，等于让所有人重装一遍。

---

## D8 · 从 019 搬什么、丢什么

**① 一字不差搬**
- `docs/data-contract.md`（见 D6）

**② 是资产，直接翻译成 TS，一句都别重写**
- `src/welcome.py` **全部文案** —— 封存语、"明天见"、随机池。**这是产品的灵魂不是代码。**
- `src/intents.py` 的规则和词集，**连注释一起搬** —— 尾部语气词剥离、为什么不敢把"开始"两个字单独做关键词，都是踩过坑才有的
- `src/diary_writer.py` 的写入逻辑 —— 同分钟合并段头、undo 时清理孤儿段头、原子写

**③ 最难重新获得的**
- `src/ilink.py` 的协议知识 → 已提炼到 `docs/protocol-notes.md`

**④ 直接丢掉**
- `start.bat` 的 Python 探测（插件不需要）
- 代理"三重保险"（Obsidian 的 `requestUrl` 自己处理网络）
- `config.py` 的 `.env` 解析（改成设置面板）
- `paths.py` 的 legacy 迁移
- Windows SSL/KeyboardInterrupt quirk（TS 不会遇到）

---

## D9 · 一天要有句号：告别语=收尾、夜间提示教在开头、「记：」逃生口（2026-08-19）

**起因**：谷雨实测一天记 8 段后发「好，结束」被当内容记进第 8 段，再发「结束」才封存。
感受是"只有'记下来啦 第 N 段'的复读，没有句号感"。9 条收尾语/告别语因为没人发光杆「结束」几乎从未出现过。

**决策**（三视角审稿后拍板）：
1. **识别层认人话**：前置应答词（好/好的/嗯/那/OK）剥掉后**只做词表精确匹配**、不走任何前缀兜底
   （「好早」「那完了」「好的撤销了订单」留在内容侧）；尾部剥 emoji/符号；复读折叠对全部命令生效。
2. **告别语=收尾，只收明确对 bot 说的**：晚安 / 我睡了 / 去睡了 / 睡觉去了 / 今天就到这(里/儿) / 今天先到这(里/儿) / 明天见。
   **光杆「睡了」「睡觉了」「先这样」「就这样」「到此为止」「写完了」不收**——记爸妈病历/宝宝作息的用户拿它们当状态记；
   标准同「在吗」：可能是内容就不收。告别语**回以同类**（"晚安 🌙 今天 N 段都收好了, 明天见"），不走「结束」的仪式池；
   空日子只道别，不接"想记什么直接发"。
3. **提醒不做推送、不按段数**：段数多的恰是最不需要收尾的备忘录用户；bot 只在你说话时说话。
   改为**当天第一条深夜（22 点起，`nudgeNightHour` 可改 data.json）消息**的回执附一句
   "(睡前跟我说声「晚安」, 我给今天收个尾 🌙)"——教在开头不在中途打断；一天一次、终身最多 3 次、
   用户手动收尾过一次就永久闭嘴；开页那条并进 tips，一条回执只挂一个括号。
4. **「记：xx」逃生口**：剥掉前缀强制原样落库，与「撤回」对称（一个救误记，一个救误吞）。
   有了它，任何词想当内容记都有路可走，词表争议的代价大幅降低。
5. **不加"明天记得来找我哦"**：像 App 留存话术；现有"明天见 / 明天我等你"够了。
6. **每条回执"记下来啦~ 第 N 段"不动**：N 是送达确认，随机化侵蚀信任。"不生硬"靠一天三个节点
   （开页 / 深夜那句提示 / 晚安）各说一次话，不是给每条回执化妆。

**顺手修的三个旧 bug**：finalizeDay 改三态（sealed/already/empty/error），跨天"已自动收尾"告知只给真自动封的
（此前用户昨晚手动「结束」了今早仍会被告知"自动收尾"）；封存后「撤回」保住封存行（撤光才连封存行一起清）；
`startsWith("撤回")` 兜底收紧为复读/指代尾巴（「撤回申请」「撤销订阅」回归照记）。

**落地后的第二轮审稿（26 个 agent 对抗复核）补的细则**：
- 「X啦」=「X了」等价（「我睡啦」「记完啦」）；前置应答词最多剥 3 层，形态补 好啊/好哦/好哈/好的呀/嗯呐、好+/行+（「好的好的晚安」「好好好结束」）；
  多段告别按词表贪心切词（「晚安 晚安」「晚安，明天见」「我睡了晚安」「好了，晚安，明天见」），任一段有词表外的字就是内容；
  头部 emoji 也剥（「🌙晚安」），书名号《》不剥（「《晚安》」是在记一首歌）；长度闸门按剥完噪音的正文算（「撤回！！！×14」仍是命令）。
- 第一人称睡觉变体补进告别词表：我先睡了 / 我该睡了 / 我睡觉了 / 我要睡觉了 / 我先去睡了 / 去睡觉了 / 晚安了 / 晚安明天见。
- **019 传下来的光杆「完了」移出 FINALIZE 词表**：它更常是感叹（「完了完了」「完了😭」），尾部剥 emoji/复读折叠上线后必被误吞——按本条"可能是内容就不收"的标准处理。想收尾说「结束」。
- 「撤回/撤销/删掉/删除」的指代尾巴白名单补齐（上面那条/那个/刚才/刚才发的…），复读间允许语气词（「撤回吧撤回吧」）；应答词剥壳后也走尾巴白名单（「好的撤回上一条」）。
- 空日子说了「晚安/结束」也计 finalize_count（说明会用了，提示永久闭嘴）；夜间提示的计数在**回执发送成功后**才落账（发不出去不烧额度）；跨天告知那条不挂夜间提示（顺延到下一条，一条回执一个括号）；
  「晚安」+图片同条先记图再收尾（数字一致）；只有 1 段时说"今天这一段收好了"；首次见面那条不再重复"说错了发「撤回」…"（欢迎语里已有）。
- 谷雨 8/19 实机第一轮反馈补的：「继续记录/继续/接着记」等宣告与「开始记日记」同类——回"直接发就行~（「结束」只是收尾标记, 之后发的照样记）"，不落库；长句「明天继续记录血压」照记（精确匹配不做包含）。
- 顺手修的取名层旧 bug：STRIP_CHARS / CLAUSE_SPLIT_RE 里全角标点原来丢了（「叫我小明！」会把感叹号存进名字）。
- **记录在案、未改（产品裁决）**：取名轮第二条 ≤10 字的短句（「今天好累」「买菜」）会被裸兜底当成名字吞掉，违反"取名永不吞内容"——需谷雨定：裸兜底命中时是否同时把原句落库，或收紧兜底长度。

**状态字段**：`profile.finalize_count` / `profile.nudge_count`（跟人走），`session.nudged_date`（逻辑日），`settings.nudgeNightHour`（默认 22，无 UI）。
契约文件格式不变（封存标记同一格式，触发方式不影响格式）。测试 311 断言。

---

## D10 · 提醒复活 + 管道补全：文件/视频/语音兜底（2026-08-19）

**背景**：公众号用户想"转发文件/聊天记录给 bot"（§见 protocol-notes §8：转发是腾讯的墙，但对话框里直发文件/视频协议全支持，020 一直静默扔掉）；谷雨定优先级：**提醒 > 语音兜底 > 文件（含视频）**，并澄清定位：**这是笔记不是网盘，文字为主图片为辅**——但"发出去=记下了"对所有类型成立，静默扔掉肯定不行。

**提醒（谷雨拍板）**：默认**开**；默认 21:30（可改，设置页有 UI）；**今天（逻辑日）什么都没记才提醒**，记过就不打扰；一天最多一次；文案多版本**轮流**（不是随机）；**连续 3 天没写就闭嘴**，用户一回来（写任何东西）计数清零、提醒恢复。

**实现原则（019 的教训，D3）**：
- 019 的 `_is_token_fresh`"20 小时窗口"是编的，判定不新鲜连试都不试——**020 到点就发、零预判、按返回码记录**（`session.reminder_last_result`），真实送达窗口发着发着就知道了。
- 插件只在 Obsidian 开着时活着：到点没开，之后打开**补发**（同一逻辑日内、还没记、没提醒过）。
- 防误提醒：发送前提是**最近一轮长轮询已完成**（`_pollSettledTs` 3 分钟内）——电脑刚唤醒时今天的消息可能还没补收，countDay=0 是假象。
- 防双发：先记账（`reminded_date`）再发送，失败当天不重试。
- README 诚实写明"只在电脑开着 Obsidian 时发得出"。

**语音兜底**：转写失败（`voice_item.text` 空）从"请重说"（=内容丢了）改为**下载原始音频存 attachments**（🎤 前缀块），回执讲清"没转出文字、原音频已存、可重说"。格式按 magic bytes 嗅探（SILK/mp3/ogg/wav/amr），认不出按 encode_type 兜底。SILK 在 Obsidian 里放不了，但"什么都别丢"优先，转码工具在 vault 侧。

**文件/视频接收**：类型 4/5 落 attachments，保留原文件名（消毒后加日期时间前缀），当天笔记插 wikilink 块。
- **md5 双重用途**：①解密校验——解出来对不上就拒收（别把密文写进库）；②**重复文件复用**——微信 CDN 对重复内容复用旧密文却发新随机 key，重发同一文件必解密失败（官方仓 #193），020 靠"md5 见过→直接引用本地那份"绕过（`ilink.fileMd5s`，上限 200 条）。
- 超 100MB（协议上限）不下载直接告知；解密可疑时回执教用户"改个名再发"。
- 「晚安」+媒体同条：媒体先落库再收尾（与图片同规则）。

**数据面**：settings +`reminderEnabled`/`reminderTime`；session +`reminded_date`/`reminder_streak`/`reminder_idx`/`reminder_last_result`；ilink +`fileMd5s`。契约新增：附件块 = wikilink 块（文件/视频/语音兜底同图片），语音兜底块带 `🎤 ` 前缀（v1.2 追加说明，格式无破坏）。

**落地后审稿轮（16 agent 对抗复核，13 条全坐实全修）**：设置页时间校验与 reminderDue 统一（`REMINDER_TIME_RE`，越界回退+Notice，半截输入不落盘）；重扫码换管道时 `_pollSettledTs` 归零（防拿旧管道的新鲜度误发）；提醒在途时的新写入不被 streak++ 覆盖（`_writeGen` 写入代数）；开关关着不再每分钟读文件（countToday=0 预判）；说过「晚安/结束」的空日子当天提醒也压掉；视频版失败文案分型（🎬，不教"改名再发"）；磁盘满说"存附件失败"不说图片；`attachmentPathNamed` 超长扩展名/全毒字符名/字节上限三处补洞（防 ENAMETOOLONG 误报为网络问题）；取名轮「晚安」+文件同条也先写后封。**记录在案未修（接受）**：countDay 读文件异常时按 0 算，极端情况下可能多发一条提醒（无害）。测试 384 断言。


---

## D11 · 取名轮退役：首次见面不再问名字（2026-08-20）

**决策（谷雨）**："名字很多时候我们也用不到"——首次见面**不再问名字**。她原本想用微信昵称打招呼（"嗨, 谷雨~"），调查结论：**协议层拿不到**（见 protocol-notes「用户资料不可得」：登录与消息里只有匿名 `<hex>@im.wechat`，无任何资料接口，这是腾讯的刻意设计）。方案二选一后她选 **B**：
- 删掉首次见面的取名问句与整个 awaiting_name 状态机（欢迎语直接进正题）；
- **「叫我XX」保留**为低调后门（帮助里有一行），已设称呼的老用户不受影响；
- 老 data.json 里滞留在 awaiting_name 的用户 onload 时迁移为 active。

**顺带销账**：D9 挂着的"取名轮吞内容"裁决项（新用户第二条短句被当名字吞掉）随 awaiting_name 分支删除而消失。取名规则引擎里 extractName/isRefusal 退役，validateName/REFUSALS 保留（「叫我XX」还在用）。协议的匿名性可作隐私卖点："bot 连你的微信昵称都拿不到，只认识一串匿名编号。"

---

## D12 · 语音原声保存 + 微信样式气泡（2026-08-20）

**需求（谷雨朋友）**：语音以语音形式保存——Obsidian 里像微信一样：上面是可点击播放的语音条，下面是转好的文字。**谷雨拍板**：做成开关（默认关，需要的人打开）；气泡要微信那种熟悉感。

**数据形态**（契约友好，零锁定）：开了开关后，一条语音消息 = 一个块：
```
🎤 ![[日记/attachments/2026/2026-08-20-1030-语音.wav]]
今天试了新的手冲豆子, 花香很明显。
```
仍是标准 markdown + WAV 文件——手机端/卸载插件后是 Obsidian 原生播放器，照样能听；文字照旧可搜索；「撤回」整块一起撤（音频文件保留，同图片规则）。

**解码**：微信语音是 SILK v3，Obsidian 播不了。内嵌 silk-wasm@3.7.1（MIT © idranme，官方微信插件同款；讽刺的是官方把它放 devDependency 标准安装装不上，线上实际没在转码——我们内嵌后比官方完整）。wasm 以 base64 内嵌（main.js 190KB→541KB），两处 `silk_default()` 打 wasmBinary 注入补丁短路文件/网络加载（Obsidian 渲染进程兼容，纯 Node 环境双验证）。输出 24kHz/16bit/mono WAV（官方口径），约 2.9MB/分钟。微信 SILK 带 0x02 前缀，规范化成恰好一个（无前缀补、双前缀不会发生）；非 SILK（mp3/ogg）不硬解，走 D10 原始字节兜底。

**行为规则**：
- 转写文字**照走意图识别**——语音说「结束」还是封存命令，不会被存成音频块；只有"记录"意图的语音才存原声。
- 任何失败（下载/解码/写盘）**静默降级纯文字**——文字是本体，音频是增强，回执照常，不打扰用户。
- D10 的转写失败兜底同步升级：先试解码成可播的 WAV，解不开才存原始字节。
- 回执文案不变（"🎤 记下来啦~ 今天第 N 段"），开关是显式选择，不用每条重复确认。

**气泡渲染**（仅桌面端装了插件时；样式层，不碰数据）：
- 识别 `…-语音.wav` 的 embed（只动本插件写的附件，用户自己的 wav 不碰），替换成微信样式气泡：绿底、喇叭三弧线图标、时长"4″"、宽度随时长小幅增长；点击播放/暂停，播放中三弧线循环点亮动画；同时只放一条（微信行为）。
- 阅读视图走 registerMarkdownPostProcessor；Live Preview 的 embed 不走它，用 MutationObserver 兜底（dataset 标记防重复处理）。亮暗主题各配色。
- 手机端无插件（isDesktopOnly）→ 原生播放器，能听没气泡，README 写明。

**成本与账**：main.js +350KB（一次性）；每分钟语音约 2.9MB 计入 vault、被同步盘带走——所以默认关。MIT 声明已在内嵌段落注明。

**落地后审稿轮（18 agent，15 条全坐实全修/记录）**：气泡渲染五处重做——命名正则收紧到本插件完整格式（「英语语音作业.wav」不再误伤）、阅读视图"气泡+原生播放器并存"（实测确定性时序而非竞态）改为 append+CSS 隐藏原生、共享单个播放器（LP 重建 widget 不再攒 Audio/孤儿续播，时长改从文件大小算）、metadataCache 未就绪不打标（留给下轮重试）、键盘/读屏可达（role/tabindex/keydown/aria 状态/focus 环/reduced-motion）；observer rAF 攒批；「叫我XX」称呼收紧 ≤4 字（「叫我妈过来吃饭」照记——存量洞，D11 后成了唯一取名入口才修）；文字+语音混合条也存原声；垃圾 SILK 解出 <0.2s 噪声按失败算（兜底保原始字节）；wasm 坏时的 unhandledRejection 已消（ba.catch）；首条非记录文字+媒体同条的 tips 重复已剥；设置 desc 消歧（气泡不随开关关闭）；死常量清理。**记录在案未修（接受）**：升级瞬间恰好停在旧版取名轮的用户，其回答「跳过/名字」会被当内容落库（一次性窗口，内容多记不丢）；弹出窗口(popout window)里 LP 模式暂无气泡（原生播放器兜底）。测试 405 断言。


---

## D13 · 路径可配置 + 写进已有的每日笔记（2026-08-30）

**起因**：GitHub issue #1（royxue，2026-08-27/28）两条留言：①路径写死 `日记/2026/2026-08-27.md` + `日记/attachments/2026/`，他的每日笔记是年/月两层、附件有专门文件夹，要可配；②最好和他的每日笔记是同一个文件（一天一个，不是所有天一个），他知道各人模板不同、不指望插件适配，建议插件固定追加自己的一个区块。
**谷雨 2026-08-29 拍板**：做「写进已有的每日笔记」开关；用标题节，不用代码块（代码块里图片不显示、语音气泡和链接都不能用）；节的默认标题「微信随手记」；**对不开开关的用户零影响**；本轮先于「切换文件夹」（#14）发版，版本 0.4.0。设计终稿 `15-daily-note-mode-draft.md`（v3），审稿全文 `15-review-round1.md`。

**决策**（终稿 §2 / §4 / §9）：
1. 设置项：`pathFormat`（moment 格式，默认 `YYYY/YYYY-MM-DD`；字母是日期代码，文件夹名放方括号）；`diaryFolder` 填 `/` = 库根目录；`attachmentMode` 三选一 diary / obsidian / custom，custom 用 `attachmentFolder`（输入框点开即列整棵文件夹树, 点选; 级联下拉被谷雨否掉）+ `attachmentSubFormat`（可选的按日期子路径）；开关 `sharedDailyNote`（默认关）；`sectionHeading`（默认「微信随手记」，固定二级，用户打的 `#` 剥掉并 Notice）；`templatePath`；按钮「从每日笔记设置导入」（Periodic Notes 优先，否则核心 Daily Notes，要看 `.enabled`；导入前确认框）；`dayStartHour` 加 UI（0–12，只在开关开时显示，默认仍 4）。
2. 格式校验：令牌白名单只放日期类 moment 令牌，时分秒与 AM/PM 一律拒；渲染同一年 366 天 + 次年 1 月 1 日互不相同；输入框下方实时预览「今天会写到: …」，不合法时预览行变红、不落盘、不弹 Notice（B3）。
3. 节的定义：`## <标题>` 起，到下一个任意级别标题行或文件末尾；代码围栏内不算；frontmatter 无闭合行则从头扫；同名取第一个；标题比对要 escapeRegExp。
4. 写入 / 计数 / 撤回 / 封存只看节正文；`_editDay` 共用模式返回节正文，回执的「第 N 段」只数节内；节内不写 frontmatter 和 `# 日期`；封存行在节内。
5. 只有写入会建文件、建节；撤回、封存、计数遇到没文件或没节直接返回现状，不建、不套模板；fn 结果与原文相同不落盘。
6. 撤到空只清正文，节标题不删（标题可能在用户模板里）。
7. 建文件一次 `vault.create`：模板已含同名标题就填进去，没有就末尾追加；空壳文件按"有文件无节"不套模板；插件建的文件 1.5s 后复核补写，复核带写入代数，窗口内撤回 / 结束过就作废。
8. 转义只在共用模式：节内 `^#{1,6} ` 行与代码围栏行（``` / ~~~）行首加 `\`，挂 `_appendBlock`；独立模式仍只首行 `# `。
9. 共用模式节正文 CRLF→LF 归一化后再交给 fn；节外字节不碰；独立模式不动。
10. 外来文件护栏（B1）：独立模式下 frontmatter 无 `source: wechat-diary` 的文件，写入照常、计数只数我们段头后的块、段头后没有块时撤回拒绝（有块照撤，与 0.3.1 一致）且不写封存行；注意 0.3.1 的「打开今天的日记」建的空文件被用户先手写过也属此类，不是"老用户永不触发"；关开关或保存格式时探测到外来文件弹确认 + 「改回默认」。
11. 「打开今天的日记」命令在共用模式下先按模板建文件再打开；附件 obsidian 模式走 `fileManager.getAvailablePathForAttachment`（先建当日文件再算路径）；语音气泡识别改成只看文件名。
12. 文案：首条回执带逻辑日日期与节标题；跨天语带日期；帮助加「这一节归插件管，节下面写任何标题就算节结束」。
13. 明确不做：适配用户模板结构；代码块 / callout；Templater 模板处理；搬迁已有文件与迁移旧标题下的内容；分区（#14）的共用模式；独立模式 CRLF 归一化。

**审稿轮**（三视角 × 三驳斥，13 agent）：30 条发现，驳倒 1，存活 29；合并为 18 条直接改 + 3 条拍板 + 8 条记录在案。最硬的 6 条：
- 撤到空连标题一起删：删的是用户模板里那一行，下一条从此跑到文件末尾。改为标题不删。
- Templater 新建触发器约 300ms 后 read→modify 整篇写回：连发第 2/3 条被快照抹掉，回执却已说「记下来啦」。改为一次 create + 1.5s 复核补写。
- moment 令牌乱码：`Assets/YYYY` 真渲染成 `AM006t0/2026`，草案的校验和测试桩都放行。改为白名单 + 示例带方括号 + 桩实现 `[..]` 字面量。
- CRLF 节正文 `countMessages` 永远 1，「撤回」一次撤光整节。改为节内归一化。
- 转义扩到独立模式违反零影响：老用户两行消息第二行 `## 计划` 会从原样变成 `\## 计划`。改为只在共用模式。
- 跨天封存在用户昨天的每日笔记里建空节、昨天没文件时还替昨天套模板建一篇。改为撤回 / 封存 / 计数不建。
谷雨终审补的 6 条：复核 vs 撤回竞态（复核清单(建文件后窗口内写进节的每一块都登记, 最后一条写完 1.5s 后缺哪条补哪条; 窗口内被撤回的块移出清单; 封存不作废——审查轮修订)，窗口内撤回过就作废）；节标题撞用户已有标题（desc 警告 + 打开开关或保存标题时探测今天文件、有内容弹确认）；导入按钮一键覆盖三项要确认框；同步冲突面（iCloud / 坚果云冲突副本里找不到节，写进 README「诚实的限制」）；frontmatter 无闭合行不能整篇跳过 + 标题拼正则要转义；老用户边角（早已把 Daily Notes 指到 `日记/YYYY/YYYY-MM-DD` 的人会命中护栏，「撤回」从"能删且会误删"变成"拒绝并说明"，记录在案）。
三道选择题谷雨 2026-08-29 拍板：B1 护栏 + 设置页提示都做；B2 开关开时露出「一天从几点开始」，默认仍 4；B3 校验呈现用实时预览。
测试库实测轮（8/31，谷雨亲测 4 项手测）补两修：①打开开关自动对齐——读每日笔记插件的 folder/format，不一致直接弹导入确认，读不到提示自己选（否则用户不点导入就写出一棵平行树，谷雨与搭测试库的 Claude 都踩了）；②关开关的外来文件弹窗对"共用模式自建的每日笔记"改为「知道了」型说明、不提供改回默认（谷雨点了改回默认致路径格式被改、文件散两层级）。另记录：核心每日笔记 {{title}} 渲染完整格式串、我们渲染文件名，保持不改。

**灰度**：本轮改的是所有写入路径，是 0.3.0 以来最大改动。先发 `0.4.0-beta.1` Release（三件套照发），**main 的 manifest.json 不 bump**，商城看不到；在 issue #1 请 Roy 用 BRAT 装预发布版，共用模式在他真实的库里跑几天（他的模板 / Templater / 附件设置我们造不出来）；谷雨生产库 用 0.3.1 的 data.json 原样启动同一版验零影响，当天文件 diff 为空。都没问题再 bump manifest 正式发 0.4.0。

**状态字段**：settings +`pathFormat` / `sharedDailyNote` / `sectionHeading` / `templatePath` / `attachmentMode` / `attachmentFolder`；`dayStartHour` 已有，加 UI；`diaryFolder` 允许 `/`。默认值下路径与文件内容和 0.3.1 逐字节相同。契约 v1.4：路径布局是默认值、共用模式节的定义、共用模式转义规则；019 只支持默认布局，按 D6 同步文档。

**落地后审稿轮**（13 agent：零影响 / 共用模式正确性 / Obsidian API 与设置页 / 文档一致性 四维度 × 两驳斥，汇总 `15-review-round2.md`）：31 条发现驳倒 1 存活 30，合并为必修 7 + 应修 8 + 记录在案 9，必修/应修全改：消息里的代码围栏行也转义（否则奇数个 ``` 让节吞掉用户内容）；复核从"只盯第一块 + 任何撤回/封存作废"改成"清单：窗口内写进节的每一块都登记、缺哪条补哪条、被撤的移出清单、封存不作废"；外来文件护栏从"整文件拒撤"改成"段头之后有块照撤、无块才拒"（0.3.1 的「打开今天的日记」建的空文件被用户先手写过的也没有 frontmatter，"老用户永不触发"被证伪）；外来文件无块不封存（否则往用户昨天的笔记塞封存行且「在吗」0 段与「晚安」3 段矛盾）；帮助文案按 dayStartHour；模板缺失/读失败每天 Notice + 设置页标红 + 改路径后清除；独立模式 + 附件「跟随 Obsidian」当天文件不存在也先建（否则落库根）；纯数字令牌固定 en 地区（ar/fa/hi 界面下 moment 换本地数字会破坏零影响）；「日记文件夹」desc 改写、改它时预览刷新；`_root` 去掉多余 trim；设置页去抖定时器重绘前先提交；「改回默认」确认框列出会改的项；导入失败 Notice 措辞。记录在案：未闭合围栏在节标题之前时末尾另建一节；模板节标题下占位文字被首条替换；frontmatter 未闭合按 Obsidian 规则；BOM 首行；README/契约补外来文件规则与围栏定义。

**测试**：578 断言（0.3.1 原 405 + 【G】黄金文件回归 37 = 0.3.1 字面基线 + 【H】共用模式/护栏/附件三模式/复核清单/围栏/本地化 136）。生产库手测清单 4 项（模板占位符对拍、附件设置四种 × 当日文件有无、编辑器打字中写入、Templater 连发）在灰度前做，结果回填终稿 §6。


---

## D14 · 两个默认值翻转 + 0.4.0 发版（2026-09-02）

**起因**：发 0.4.0 前核对发现 PR #2 合并进来的 `webClipEnabled` 默认值是 `true`，且读取写法是「没设过就算开」——与谷雨对 PR #2 的收编条件「开关默认关」、以及她 9/1 在 issue #1 里答复 Roy 的「默认关，设置里可开」都不一致；0.3.1 用户升级后一发公众号链接就会自动剪藏，违背「发什么记什么」。
**谷雨 2026-09-02 拍板**：①剪藏默认关（`webClipEnabled: false`，两处读取改成 `=== true`，设置页说明加「默认关闭」）；②「保存语音原声」默认开（翻转 D12 定的默认关；设置页与 README 改成"默认开，可关"）；③不等 Roy 对 beta.4 的反馈，直接发 0.4.0（beta.4 的 main.js 与 main 逐字节一致，正式版只多这两处默认值）。
**影响面**：老用户升级后——剪藏保持不开，与 0.3.1 行为一致；语音消息开始额外落 WAV（约 3MB/分钟），不想要的在设置里关。她自己的生产库 data.json 两个键都没设过，随默认值走。

## D15 · AI 每日总结：边界生成 + 早上推送（2026-09-10）

**需求**（谷雨 2026-09-10）：「加一个 AI 功能，每天晚上 12 点到当天的笔记总结。」

**澄清后的四条拍板**：

1. **生成时机 = 逻辑日边界**（默认凌晨 4 点，跟随 `dayStartHour`），总结**刚结束的那一天**。
   她原话是「晚上 12 点」，但插件的一天按契约 v1.2 从凌晨 4 点切：00:00 时逻辑日还没结束，
   零点总结会把 00:00–04:00 熬夜写的部分漏掉。她选「延迟到边界」——宁可晚生成，也要完整。
2. **产物落两地**：写进当天笔记的 `## AI 总结` 一节 + 推一次微信。
3. **素材只取微信随手记记下的内容**：共用每日笔记模式下，用户在同一个文件里自己写的东西**不发**给接口。
4. **微信推送挪到早上**（可配，默认 08:00）：生成在凌晨 4 点，但凌晨 4 点震手机是事故。

**为什么值得做**：D2 早就拍过「020 内建回顾能力」——B 类用户没有自己的 Agent，只存不回顾的产品
留不住人；D2 列的三条价值里，「昨天你说的那个面试，后来怎么样了」这类**主动**回顾只差一个触发器。
基础设施（`AiClient`、OpenAI 兼容设置项、Key 存 `SecretStorage`）在 0.3.0 就留着了，这次是接上它，
不是新建一套。技术上也确实便宜：日记就是本地 md，读 vault 是插件原生能力，不需要向量库/RAG/服务器。

**默认关**，且必须用户自己配 API Key。理由：正面兑现 D2 记下的那条代价——「零 key 也完整可用」
是 019 的承诺，020 里改述成「零 key 时 AI 功能不存在，但记录链路完全不受影响」。把笔记内容发给
第三方接口必须是显式选择，不能靠升级默认打开（D14 那次默认值事故的教训）。

**对记录区的零影响是硬约束**：总结节**固定放文件末尾**、由插件独占、每次覆盖重写。
`_appendRaw` / `undoLastBlock` / `finalizeDay` / `_count` 在入口 split 一次、出口 join 回去，
记录区的既有语义（只追加 / 撤回 / 封存 / 段数）完全不需要知道它存在。模型输出还要过一层消毒
（转义形如 `**09:30**` 的整行与模型自加的 `#` 行），否则它会伪装成段头、或抢走节的边界——
这两样都会破坏字节保真。**这是本插件唯一不适用「只追加、历史段落永不改写」的写入**：
总结是可再生的派生内容，重算同一天必须替换而不是再堆一份；所以它独占文件末尾，与记录区物理隔离，
README 与 data-contract 里对这条例外都要写明。

**代价 / 已知限制**（写进 README）：

- 只在 Obsidian 开着时跑。电脑关着就顺延到下次打开——逻辑日时钟上「到点」过了就不再回头，
  醒来补推正是想要的（与 D10 提醒同款）。
- 微信推送走 iLink 主动发消息，受 D3 那条 token 窗口限制。所以连推 3 次失败后降级成
  「你下次跟 bot 说话时把总结带出来」：用户开口恰好刷新了窗口，这是最可靠的送达时机。
- 每天一次接口调用；素材上限 1.2 万字（超出截断并注明），输出上限 1500 字。

**不做**：出片时段可配之外的调度花样（不补跑陈年旧账，只做刚结束的那一天）；不生成周报/月报；
不做对话式追问（`ChatHandler` 仍按 D2 的结论挂在墙上）。

### D15 补 · 「总结」关键字触发（2026-09-10，同日追加）

**需求**（谷雨）：「加一个关键字触发机制，比如我说总结，自动总结，不用每日定时总结。」

**三条拍板**：

1. **总结哪一天 = 今天到现在**（当前逻辑日）。不是"最近有记录的那天"，也不是昨天——
   行为最可预测，代价是早上发会说「今天还没记东西呢」，她接受。
2. **产物落两地**：回微信 + 覆盖写笔记的「AI 总结」一节（与定时那条路同一个节，都是覆盖式）。
3. **开关拆开**：`aiSummaryEnabled`（主开关，管"这个功能能不能用"）之下新增
   `aiSummaryScheduled`（定时子开关，**默认关**）。发「总结」只看主开关；每日定时要两个都开。
   理由：按需触发才是多数人想要的，不想要的定时不该由"打开 AI"顺带带来。

**升级影响（必须写进 Release 说明）**：`aiSummaryScheduled` 是新键、默认关，**不做迁移**。
0.5.0 上已经打开 AI 的用户升级后，「每日定时总结」处于关闭状态（旧版只要 `aiSummaryEnabled` 开着
就会跑定时）。方向是有意选的：默认关落在隐私安全一侧，不会凭空产生 API 调用或凌晨推送；
要定时的人回设置里打开一次即可。反过来把旧的 `aiSummaryEnabled: true` 迁移成定时也开，
等于让 0.5.0 用户的凌晨定时悄悄复活——而这正是本次要拆掉的默认行为。
这条与 D14 的教训同源但方向相反：D14 是"新默认不能替用户把功能打开"，这里是"新默认可以替用户关上，
但必须说清楚"。

**实现要点**：

- 新命令词 `总结` / 总结一下 / 今天总结 … 走既有的 `INTENT` + `matchCommand` 精确匹配，
  长度闸门 + 精确匹配天然挡住长句（「今天开完会写了个总结」是内容）。**故意不收「小结/复盘/回顾/摘要」**
  ——它们更容易是内容；想记「总结」这个词本身用「记：总结」逃生口。与「在吗」同理：命令不落库。
- **刻意不碰 `summary_last_date`**。那是定时那条路的账；按需总结若去写它，当天边界的定时总结会
  误判成"已处理过"而跳过——白天问过一次，夜里就再也拿不到那份一整天的总结。按需总结另记
  `onsummary_day/ts/blocks`。
- **防手抖**：10 分钟内、素材段数没变 → 把笔记里那份读回来复用，不再烧一次 token。
  过了窗口或期间又记了新东西，都照常重算（重新生成不该被这个闸门挡住）。
- 三种前置失败各有各的话（没配接口 / 开关没开 / 今天没记），都明说下一步做什么，不静默、不编内容；
  AI 失败不落笔，写笔记失败则把内容先给用户并说清没存下。

**没做**：不做「总结昨天」「总结某一天」的日期参数——需要时再加，现在加会引入日期解析这一整摊
歧义（「总结上周」算不算？）。不做定时任务的删除，只把它默认关掉、留成可选。

**版本号**：0.5.0。注意 `15-daily-note-mode-draft.md` 的原计划把 0.5.0 留给了 #14「切换文件夹」，
本轮先用掉，#14 顺延到下一个版本号。仓库的发版惯例是只 bump `manifest.json` + `versions.json`——
`main.js` 里的 `PLUGIN_VERSION` 是协议头里的 agent 串，自 0.3.0 起不随发版变动（0.3.1 与 0.4.0 两次发版均未动）。

**0.5.0 同时是仓库的第一个 tag + Release**：0.1.0–0.4.0 都只 bump 了 main 上的 `manifest.json`，
从未打过 tag（`15-daily-note-mode-draft.md` 里写的「三件套照发」当时只是计划，没执行）。
从这版起走 Release 分发：tag `0.5.0` 指向 `808c1fb`，Release 附 `main.js` / `manifest.json` / `styles.css`
三件套（已核对与仓库文件逐字节一致），BRAT 可以按 `burndown/obsidian-wechat-diary` 装了。
后续版本沿用这条路径即可。

---

## D16 · 自定义提示词：多条、各有触发词与自己的节（2026-09-10）

**需求**（谷雨）：「给插件加一个自定义提示词功能，而不是只是总结。」

**澄清后的形态**（一次问了三个问题，其中两次答得自相矛盾——粒度选了"只一条"、落点与内置处理却按
"多条"回答——回头问第三次才定下来）：
1. **多条**，不是"一个可编辑的文本框"。每条 = 触发词 + 提示词 + 写进笔记的节标题。
2. **每条写自己的节**，所以「待办」「周报」「总结」可以同时留在笔记里，互不覆盖。
3. **内置「总结」保留**、可用不可删——它是「每日定时总结」那条路的固定出口；自定义的是额外的。

**为什么不做成"只让总结提示词可编辑"**：那样同一时间只能有一种产物，用户想要第二件事就得把第一件
改掉。而插件的节扫描本来就是"按标题认节"（D13/#15 那套），多开几个 AI 节几乎不额外花代价。

**两个必须硬拦的地方**（数据安全，不是体验）：
- **触发词不许撞内置命令词**。运行时顺序已经保证内置优先（自定义只在 `detectIntent` 判为"内容"之后
  才被问到），所以手改 `data.json` 塞一个「撤回」也顶不掉撤回；但保存时必须当场拒掉并说明原因，
  否则用户会以为"我明明设了却没反应"。
- **节标题不许撞**记录节、内置总结节、或别的自定义节。撞了 `locateSection` 就认不出谁是谁，会当场
  搞乱记录区。设置页拦一道，`_summaryHeading()` 那种兜底再来一道。

**这轮抓出并修掉的一个真实缺陷**：D15 把"记录区 = 内置总结节之前的一切"写死在**一个标题**上。
自定义节可能**先于**内置总结节被创建（用户先发了自定义触发词），那时它会落进"记录区"——
段数把它算进去、新记录被追加到它后面、撤回还可能删掉 AI 产物。
改成 `splitRecordRegion(content, _aiHeadings())`：记录区 = **最早出现的那个 AI 派生节**之前的一切；
`_count` / `_appendRaw` / `undoLastBlock` / `finalizeDay` 四处统一改用它。
这条不是设计时想到的，是被 §D16.10 那组回归用例抓出来的。

**提示词容错**：用户不写 `{entries}` 时把记录自动接在末尾——否则他会得到一个"模型根本看不到记录"的
提示词，而且失败得不响（静默编一个结果）。素材仍是**今天到现在**（与「总结」一致），读之前摘掉
**全部** AI 派生节：少摘一条就会把别的 AI 产物当成"用户原话"喂回去。

**已知约束（写进 README 与契约 v1.6）**：AI 节按 Markdown 的节边界规则，身体一直到"下一个任意级别
标题"为止（最后一个节到文件末尾）。所以**写在两个 AI 节之间、或最后一个 AI 节之后的内容算在该节体内**，
重算时会被覆盖。这是"插件独占的派生节"这个设计的直接后果，代价是"别在那里手写"。
要根治得给每个 AI 节加结束标记（HTML 注释），让覆盖只到标记为止——那是写入格式变更，本轮没做。

**没做**：按条目配模型/温度/超时；条目拖拽排序；给条目加"跑哪一天"的参数（`总结昨天` 那类日期解析
仍然是整套悬着的歧义）。

---

## 已知的 019 待修问题（020 别照抄，019 以后也要修）

从代码通读中发现的，按影响排序：

1. **提醒时钟机制必然失效** —— 见 D3。这是最严重的一个。
2. **午夜割裂**：23:58 进 diary 模式，00:01 继续说 → 跨天判定静默回 chat 模式，
   这句话不记了，用户只收到一句闲聊回复。（`019Dairy/src/session_state.py` 的
   `load_or_reset`）熬夜写日记的人一定会踩到。
3. **chat 模式下误切换会吃掉内容**：`intents.py` 的子串匹配优先级最高且不受长度限制，
   「从下个月**开始记录**我的开销，感觉钱花得莫名其妙」→ 判成切换指令，切了模式，
   **这句话本身不写入**。切换的同时把触发切换的那句话丢了。
4. **USER_ID 要用户手动从终端复制到 .env** —— 程序明明自己已经拿到了
   （`login()` 里的 `state["ilink_user_id"]`）。020 不存在这个问题（设置面板自动写）。
5. **成本提示每条都追加**：chat 模式第 2 条起每一条回复都附"闲聊会消耗 token"，很烦。
6. **import 顺序的隐式依赖**：`ilink.py` 模块加载时清空所有 proxy 环境变量，
   而 `diary_writer.py` 模块加载时读这些变量探测代理。现在能工作纯粹因为
   `main.py` 的 import 顺序恰好正确。（020 不存在此问题）

---

## 挂起问题

### ✅ 调研已回答（2026-08-05）

| 问题 | 答案 |
|------|------|
| iLink 协议有没有更新？ | **有，而且找到了权威来源** —— 腾讯官方开源 `@tencent-weixin/openclaw-weixin`（npm，MIT，含完整 TS 源码）。019 现有实现与官方有 **5 处实质偏差**，详见 `protocol-notes.md` 第 1 节 |
| `channel_version` 最新是多少？ | 2.4.6（019 用的 1.0.2 过时三个大版本，目前还跑得通；服务端校不校验查不到） |
| 20 小时限制是官方规则吗？ | **不是，是编的。** 全部官方与社区资料无一提及；代码层面可确证是本地拍脑袋的启发式 |
| 普通用户能自己创建 bot 吗？ | **能。无审批、无白名单、无资质、无地域限制，扫码即用。** 但存在账号级验证码风控（`need_verifycode` / `verify_code_blocked`），触发条件无文档 |
| `requestUrl()` 能撑住长轮询吗？ | 桌面端**没有任何应用层超时**（三层独立验证）。但**不该用它**——它强制跟随系统代理，改用 Node `https`（见 D1 补充④） |
| 社区市场审核会拒吗？ | **大概率不会。** 政策是 allow-with-disclosure，在架先例充分（含你自己的 memo-lite） |
| 移动端能跑长轮询吗？ | **不能。`isDesktopOnly: true`**（见 D1 补充③） |
| token 存 data.json 的风险 | 确实会被同步散布 → **改用官方 `SecretStorage`**（见 D5） |
| 有没有更适合的微信通道？ | iLink 是"个人可用 + 用户在普通微信 + 能收输入 + 零公网依赖"四项全中的唯一通道。企业微信推送侧无窗口，但**三个卡点全部未核查**（见 D3） |

### 🚧 P0 实测 —— 决定架构，动手写代码前应该先做

- [ ] **① 离线消息缓冲窗口有多久？** 停 5min / 1h / 6h / 24h 后用旧 cursor 重连各能拉回多少
      → **决定"Obsidian 关掉一天"可不可接受，是插件形态最大的软肋**
- [ ] **② 企业微信群机器人 webhook 能否落到普通微信？**
      → 30 分钟就能试出来；成功则整个提醒死结当场解开
- [ ] **③ 真实推送窗口。** 删掉时间预判，改成到点就发 + 按返回码记录 token 年龄

### 🔍 P1 实测 —— 影响实现细节

- [ ] 服务端校不校验 `channel_version`（同账号分别用 1.0.2 / 2.4.6 打 `getupdates` 比对）
- [ ] `ret=-2` 的准确语义（社区两份文档互相矛盾）
- [ ] "不带 context_token 发送"到底可不可用（**网传的"实测无效"已被推翻，实际是从未验证过**）
- [ ] iLink 允不允许多客户端并发 `getupdates` → 决定"Python 常驻 + 插件"能否并存过渡
- [ ] `while + await` 形式的长轮询会不会触发扫描器的 `setInterval + network` 规则
      （用开发者面板的 **preview scan** 提交前就能扫，官方免费手段）
- [ ] 019 那段 `cancel/canceled/cancelled` 分支到底对应什么（官方 8 个状态里没有它们）

### 📄 无法靠实测消除的

- [ ] 《微信ClawBot功能使用条款》的腾讯官网**原始 URL** —— 目前全文只在社区镜像仓库读到，
      合规这种高后果场景不宜引用可被增删改的二手文本。**建议在微信客户端内找到条款页核对**
- [ ] 绕开 OpenClaw 直连 iLink 是否违反条款 —— 条款既未明文允许也未明文禁止。
      **这是做这件事最大的不确定性，且它无法通过实测消除**

### 🤔 等决策

- [ ] **要不要先在 019 (Python) 上修那 5 处协议偏差、验证有效再移植？**
      调研的建议是"先修 Python 再移植"，理由是这几条都低成本高确定性，
      而且在 Python 上验证比在新写的 TS 上容易得多。
      代价是推迟 020 的启动。**这条待拍板。**
- [ ] **要不要考虑"常驻进程 + 薄壳插件"的混合架构？**
      这是唯一能真正满足"白天不开 Obsidian 也不丢消息"的架构，
      代价是普通用户要装两个东西，与"让不懂技术的人也能用"直接冲突。
      **取决于 P0 实测①的结果** —— 如果 cursor 能补拉一天，就不需要考虑这条
- [x] ~~回顾功能没有 AI key 时怎么降级？~~ → **D15 已答**：整个 AI 功能默认关、可选。
      没配 Key 时记录链路完全不受影响，只是没有总结——不做「降级成按日期列出」那种半成品
- [ ] 提醒时间每天前漂，怎么和用户解释
