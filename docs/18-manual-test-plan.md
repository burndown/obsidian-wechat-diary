# #18 多账户 · 真机测试计划（0.5.0-beta.1）

> 面向：谷雨，在自己真实的库 + 两个真实微信号上验。
> 分支 `feat/multi-account`，版本 `0.5.0-beta.1`（**预发布**，不动 `main`——`main` 仍是上游 0.4.0）。
> 设计稿 `18-multi-account-draft.md`；决策 `00-decisions.md` D15。
> 自动测已覆盖 739 条（bindtest）+ 92 条（webcliptest）；**本文件的重点正是那些自动测不到的**（§5）。

## 0. 装之前（2 分钟，别跳）

```bash
VAULT="$HOME/你的库"                      # ← 换成你的库路径
PLUGIN="$VAULT/.obsidian/plugins/wechat-diary"

# ① 备份：data.json 里是全部设置 + 两个账户的状态；迁移会重写它
cp "$PLUGIN/data.json" ~/wechat-diary-data.before-multiaccount.json.bak
# ② 记下当前版本，回退时要照着装回去
grep '"version"' "$PLUGIN/manifest.json"
```

## 1. 怎么装

**方式 A：手动覆盖三件套**（最稳，不依赖任何工具）

```bash
gh release download 0.5.0-beta.1 --repo burndown/obsidian-wechat-diary -D "$PLUGIN" --clobber
# 没装 gh 的话：浏览器打开 https://github.com/burndown/obsidian-wechat-diary/releases/tag/0.5.0-beta.1
# 下载 main.js / manifest.json / styles.css 三个文件，覆盖进 $PLUGIN
```

然后在 Obsidian 里：**设置 → 第三方插件**，把 WeChat Diary 关掉再打开（或 `Ctrl/Cmd+P` → `Reload app without saving`）。

**方式 B：BRAT**（BRAT → Add beta plugin → `burndown/obsidian-wechat-diary`，并允许预发布版本）。

装好后到插件设置页，**应该看到顶部多了一个「微信账户」区**（0.4.0 那里只有一行绑定状态）。

## 2. 随时回退（先看这条，心里有底）

```bash
gh release download 0.4.0 --repo ArtemisLin/obsidian-wechat-diary -D "$PLUGIN" --clobber
# 然后重载 Obsidian
```

回退后会发生什么，**先说清楚**：
- 你的**设置在**：`data.json` 里 `settings.diaryFolder` / `pathFormat` / 附件 / 提醒 / 剪藏都还在
  （新版的"设置垫片"是**可枚举**的，就是为了让值照旧写回 `data.json`）。
- 顶层 `ilink` / `profile` / `session` 三个键**没了**（这是设计：它们搬进了 `accounts[0]`）。
  0.4.0 启动时会走"从密钥存储恢复身份"那条路（v0.2.1 修的那个机制），**绑定能自己回来**。
- ⚠️ **如果你在测试版里重新扫过码**，回退后 0.4.0 可能拿到的是旧 token（新码只写
  `...-bot-token:a1` 这个带后缀的 key），那样需要**重新扫码一次**。
  想免除这点的话说一声，我可以加一条"账户 #1 同时把 token 镜像写进旧 key"（约 5 行 + 用例）。
- `profile.name`（称呼）与 `session` 里的计数在回退后会丢（0.4.0 不认识 `accounts`）。不回退就没事。

## 3. 测前状态（30 秒，用来对比）

- 截一张设置页的图（账户区 + 日记/附件/提醒/剪藏）
- 记下：今天的日记文件路径、当前段数
- `data.json` 里 `settings.diaryFolder` / `settings.pathFormat` / `settings.sharedDailyNote` 的值

## 4. 测试项

每条：**怎么做 → 期望什么 → 怎么取证**。取证尽量是"路径/文件/截图"，不要只凭感觉。

### A. 迁移正确性（最关键：老用户不能掉绑定）

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| A1 | 打开 Obsidian，看插件设置页 | 仍显示**已绑定**；日记文件夹与测试前一致 | 截图 |
| A2 | 发一条微信 | 落进**原来那个文件夹**、段数接着涨（不是新建一棵树） | 文件路径 + 段数 |
| A3 | 看 `data.json` | 多了 `accounts[0]`（含 `diary`/`ilink`/`profile`/`session`），`settings.diaryFolder` **还在** | 贴 `accounts[0].diary` 与 `settings` 的片段 |
| A4 | 看 `data.json` 顶层 | **没有** `ilink`/`profile`/`session` 三个键 —— 这是设计，不是 bug | `Object.keys` 的结果 |
| A5 | 微信发「在吗」 | 回"已记 N 段"，N 与 A2 后的真实段数一致 | 聊天截图 |

### B. 加第二个账户

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| B1 | 设置页「添加账户」 | 列表多一行、被选中（标题带 ▸）、**自动弹出该账户的二维码** | 截图 |
| B2 | 用**第二个微信号**扫码 + 确认 | 该账户变成"已绑定"，显示第二个 `userId`（与账户 1 不同） | 截图 |
| B3 | 看账户 2 的日记文件夹 | 默认是 `日记-账户2`（自动让开了账户 1 的 `日记`） | 截图 |
| B4 | 两个号各发一条不同的内容 | **各落进各自的文件夹**，互不串；两边都能看到自己的那句 | 两个文件的片段 |
| B5 | 两个号各自发「在吗」 | 各自报**自己账户**的段数（不是合计） | 两张截图 |

### C. 隔离

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| C1 | 改账户 2 的路径格式（如加一层 `YYYY/MM`） | 账户 1 的文件一个不动；账户 2 新消息按新格式落 | 改前/改后的路径 |
| C2 | 账户 2 关掉「每日提醒」、账户 1 开着；把账户 1 的提醒时间临时调到 2 分钟后，且**两个号今天都不记** | 到点只有**账户 1 收到**提醒；账户 2 一条都没有 | 两张聊天截图 |
| C3 | 账户 1 发「撤回」 | 只影响账户 1 那条；账户 2 的内容一个字不动 | 两个文件对比 |
| C4 | 两个号各发「结束」 | 各自那棵树出现自己的封存行 | 两个文件片段 |
| C5 | 账户 1 用「叫我XX」设称呼、账户 2 发「在吗」 | 称呼不互相影响（账户 2 的回复里不该出现账户 1 的称呼） | 聊天截图 |

### D. 同树拦截（这次新增的核心保护）

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| D1 | 把账户 2 的文件夹改成与账户 1 相同 | **拦下**：Notice 说"和「账户 1」是同一个…"，输入框回退，不落盘 | 截图 |
| D2 | 试 `/日记`、`日记/`、` 日记 ` 这些写法 | 都算同一个（归一化后相等），都应被拦 | 截图 |
| D3 | 设成"folder 不同但渲染路径相同"：账户 1 = `日记` + `YYYY/YYYY-MM-DD`（默认），账户 2 = `日记/2026` + `YYYY-MM-DD` | 也被拦，且提示里带**具体文件路径** | 截图 |
| D4 | 手改 `data.json` 让两个账户同 folder，然后重载 | 出一条警告 Notice（点名两个账户），但**设置页能打开、文件没被改** | 截图 + 文件未变的证据 |

### E. 增删重绑

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| E1 | 删除账户 2 | 二次确认；删完账户 1 一切照旧；**vault 里的文件一个没动** | 删除前后的文件列表 |
| E2 | 把账户 1 也删掉 | 提示说会重置为未绑定；删完列表剩**一个新的空账户**，需要重新扫码 | 截图 |
| E3 | 重新扫码绑回账户 1 | 写回**原槽**（不新建账户）；用原来的号发一条能正常落 | 截图 + 文件 |
| E4 | 在账户行点「解除绑定 → 清除残留凭据」 | 该账户回到"待认领/未绑定"，**另一个账户不受影响** | 截图 |

### F. 并发与长轮询（只有真机才测得到）

| # | 怎么做 | 期望 | 取证 |
|---|---|---|---|
| F1 | 两个号交替快发 5-6 条 | 都收到、不重复、不丢；两份笔记各 5-6 段 | 两个文件 |
| F2 | **关掉 Obsidian 10 分钟**，期间两个号各发 2 条，再打开 | 两个账户各自补收（间隔与范围符合预期） | 两个文件 + 时间戳 |
| F3 | 让电脑睡眠 5 分钟后唤醒 | 两个管道都恢复；控制台没有持续刷屏的报错 | 控制台截图 |
| F4 | 断开网络 1 分钟再恢复 | 恢复后不重复落笔、不卡死 | 文件 + 控制台 |

### G. 稳定性取证（给我这些我才能定位）

| # | 怎么做 | 期望 |
|---|---|---|
| G1 | `Ctrl/Cmd+Shift+I` 打开控制台，过滤 `wechat-diary` | 没有红色报错；有的话整段复制 |
| G2 | 看 `data.json` 里两个账户的 `ilink.buf` | 两条都在**推进**（说明两条长轮询都活着）；若某个卡住不动 → 那条管道死了 |

## 5. 自动测覆盖不到、这次重点看的地方

这些已被"故意改错看用例挂不挂"的变异验证确认过**没有自动兜底**（详见设计稿 §9.2）：

1. **行内文件夹选择器的同树拒绝**（把它改坏，739 条用例全过）→ 对应上面 **D1/D2**
2. **账户列表渲染 + 点行切换**：点标题能切、点输入框**不会**重画（打字不丢焦点）
3. **添加账户 → 立刻扫码 → 写进新槽**（B1/B2）；到第 6 个账户应被上限拦住
4. **重新扫码写回原槽**（E3）
5. **删除的二次确认** 与"vault 文件一个没动"（E1）
6. **手改 data.json 造同树的警告时机**（D4）
7. **多账户时的行高与滚动位置**

## 6. 出问题时给我什么

1. 截图（含那条 Notice 的完整文字）
2. 控制台里 `[wechat-diary]` 的**整段**文本（不是摘要）
3. `data.json` —— **先把隐私字段删掉再发**：`accounts[].ilink.botTokenFallback`、`accounts[].ilink.contextTokens`、
   `accounts[].ilink.baseUrl`。（**bot token 与 绑定身份不在这个文件里**，它们在 Obsidian 密钥存储中，放心。）
4. 你做了什么 → 期望什么 → 实际什么（三句话就行）

## 7. 测完给我结论的方式

按 §4 的编号逐条回"过 / 不过 / 没测"，不过的附 §6 的证据。**特别想知道的**：
- D1/D3 的拦截是否真的生效（这是这次唯一的"防数据混写"保护）
- F1/F2 两条长轮询并发下的表现（这是架构上风险最高的一步）
- 有没有哪一步的**期望写错了**（说明我对行为的理解有偏差，也要告诉我）
